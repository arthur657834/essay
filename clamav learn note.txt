yum -y install bind-utils openssl-devel
yum -y install clamav clamav-update clamav-server clamav-scanner

vi /etc/freshclam.conf
# Example 注释掉这一行
 
freshclam 更新病毒库

clamscan -r /home --remove -l /var/log/clamscan.log






